                                     /*Knowing is not enough,we must apply!!*/
 #include<bits/stdc++.h>
 using namespace std;                          
 #define ll long long int
 #define MOD 1000000007
 #define boost ios_base::sync_with_stdio(false);cin.tie(NULL);

 //satck is adapter container,operations happen only on one end,no iterators,
 //top to access first element,empty to find if it is empty
 //stack is used as deque as default,vector,list
 //queue is used as FIFO (First In First out),list or deque,pushed at back and poped front
// front-access element at front,back-access element at back
int main()
{ 
  queue<int>q1;
  int x;
 for(ll i=0;i<2;i++)
 {
 	cin>>x;
 	q1.push(x);

 }
 //  for(ll i=0;i<2;i++)
 // {
 // 	cout<<q1.[i];
 // }
 // while()
}